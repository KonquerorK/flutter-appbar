import 'package:flutter/material.dart';

class MenuItemSettings extends StatelessWidget {
  const MenuItemSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Menu Item Settings'),
        centerTitle: true,
      ),
    );
  }
}