import 'package:flutter/material.dart';

class MenuItemShare extends StatelessWidget {
  const MenuItemShare({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Menu Item Share'),
        centerTitle: true,
      ),
    );
  }
}